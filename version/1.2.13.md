# 1.2.13

## 本次目标
- 修正 Home 页面中两张并排图片卡片的垂直错位问题，使其顶部对齐。

## 修改文件
- `src/components/Home.tsx`
- `version/1.2.13.md` (new)

## 关键改动说明
- 在 Home 的“生活·故事”分区中，右侧图片卡片原本带有 `sm:translate-y-10`，会在 `sm` 及以上断点整体下移。
- 移除该偏移类后，左右两张卡片在同一行按网格自然对齐，保持响应式布局一致性。
- 未改动访客模式、留言板、管理员入口及任何 API 业务逻辑。

## 本地校验
- `npm run lint`：通过
