# 1.2.17

## 本次目标
- 按要求将 Home 页面回退到 `1.2.13` 状态，撤销 `1.2.14~1.2.16` 的留言展示板改造。

## 修改文件
- `src/components/Home.tsx`
- `version/1.2.17.md` (new)
- `version/1.2.15.md` (deleted)
- `version/1.2.16.md` (deleted)

## 关键改动说明
- 以历史基线 `23756d4` 恢复 `Home.tsx`，移除“留言展示板/滚动墙”相关逻辑与 UI。
- Home 第二分区右侧恢复为“出阵判定之”信息卡，不再从 Redis 抽样并滚动渲染留言。
- 页脚版本号同步调整为 `Home v1.2.13`。
- 保留访客留言板 `GuestMessageBoard`、管理员入口、帖子跳转、壁纸切换等既有业务逻辑。

## 对照自检
- 业务逻辑保留：通过
- 安全性（无新增 XSS/敏感信息暴露）：通过
- React/TypeScript 规范：通过
- 可读性与命名：通过
- 代码精简与职责：通过
- 目录与命名规整：通过

## 本地校验
- `npm.cmd run lint`：通过
