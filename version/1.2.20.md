# 1.2.20

## 本次目标
- 登录页左右组件 UI 对齐（大屏下左侧视觉卡与右侧登录卡高度、边界对齐）。

## 修改文件
- `src/components/Login.tsx`
- `version/1.2.20.md` (new)

## 关键改动说明
- 登录主网格容器增加 `items-stretch`，确保左右列按同一行高拉伸。
- 左侧视觉卡补充 `h-full`，右侧容器补充 `className="h-full"`，统一两侧高度基准。
- 右侧 `Card` 改为 `flex h-full flex-col lg:min-h-[640px]`，与左侧 `lg:min-h-[640px]` 对齐。
- 右侧 `CardContent` 改为 `flex flex-1 flex-col`，保证内容区在等高卡片内保持稳定布局。
- 本次未改动管理员登录/访客登录接口逻辑，仍使用：
  - `POST /api/login`
  - `POST /api/guest-login`

## 审美审查（Kuromi 风格）
- 保留原有黑粉青霓虹配色、毛玻璃与阴影层次，不破坏当前 Kuromi 视觉方向。
- 对齐后左右信息密度更平衡，页面中心焦点更稳，观感更接近“海报式双栏构图”。

## 业务与安全审阅
- 业务逻辑未删改：管理员/访客双模式状态、提交处理、错误提示逻辑保持不变。
- 安全边界未变化：仅前端布局 class 调整，无新增网络请求、无权限判断变更。
- 可维护性提升：通过容器级对齐类实现，避免重复样式和硬编码偏移。

## 编码与可运行性检查
- 中文文案编码已核验（UTF-8 内容正常，未引入乱码文本）。
- `npm.cmd run lint`：通过（`tsc --noEmit`）。
