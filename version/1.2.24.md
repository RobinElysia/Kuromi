# 1.2.24

## Goal
- Rewrite the `Home` workflow section into a personal tech-stack showcase while keeping existing business behavior intact.

## Modified Files
- `src/components/Home.tsx`
- `version/1.2.24.md` (new)

## What Changed
- Replaced only the `workflow` section (around line 450) with a responsive stack panel:
  - `Java`: Spring, SpringMVC, MyBatis, SpringBoot, SpringAI
  - `Python`: FastAPI, Transformers, peft, pandas, pyTorch, uv
  - `Dev`: git, Linux, Docker, Kubernetes
  - `Frontend`: Vue, React, TypeScript, vite
  - `Learning`: Machine Learning, Reinforcement Learning, Deep Learning
- Kept existing animation architecture:
  - Reused `SECTION_REVEALS.workflow`, `PROCESS_GRID_REVEAL`, `PROCESS_CARD_REVEAL`, and `activeStep`.
  - Added active-card motion (`y`, `scale`, subtle `rotateZ`) and gradient overlays in Kuromi-style neon tones.
- Kept project constraints visible in the section copy:
  - no registration
  - only `RobinElysia` and `Meow` as admins
  - nickname-based guest mode remains

## Business/Safety Review
- No API route changed.
- No auth/permission logic changed.
- No data model changed.
- Existing page flow and state behavior are preserved.

## Encoding & Run Check
- File encoding kept as UTF-8.
- Ran: `npm.cmd run lint`
- Result: pass (`tsc --noEmit`).

## Design Review Notes
- Referenced Kuromi visual assets under `src/public/kuromi` and verified style direction against `src/public/kuromi/16x9/kv_krm.png`.
- Section keeps neon cyan/rose gradients, glassmorphism layers, and animated emphasis consistent with current Home style.
