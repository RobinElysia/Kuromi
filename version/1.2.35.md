# 1.2.35

## Goal
- 按参考图重构留言板内部布局，将弹幕展示区与个人信息 / 输入发送区明确拆分为左右两栏。
- 修正留言板组件中的异常文案显示，避免乱码文本继续留在界面上。

## Modified Files
- `src/components/GuestMessageBoard.tsx`
- `version/1.2.35.md` (new)

## Changes
- `src/components/GuestMessageBoard.tsx`
  - 保留原有留言读取、昵称校验、提交到 `/api/messages`、本地更新最近 24 条消息的业务逻辑。
  - 将组件主体改为响应式双栏布局：
    - 左侧：仅负责弹幕板展示与氛围信息。
    - 右侧：展示当前昵称、最新留言摘要、规则提示，以及输入和发送表单。
  - 保留多轨弹幕实现，并继续支持 `prefers-reduced-motion` 降级为静态列表。
  - 继续移除“留言发布成功”提示，成功后只清空输入并即时更新弹幕。
  - 清理界面文案，替换异常显示文本，保证中文文案正常可读。
  - 维持现有 Kuromi 风格的毛玻璃、渐变光斑、边框高光和面板层次。

## Preserved Behavior
- 访客模式仍然只依赖昵称，不新增注册流程。
- 留言仍然写入 Redis，接口路径和后端协议未变化。
- 最近消息条数上限仍为 24。
- 留言失败时仍然显示错误提示。

## Security / Maintainability
- 未新增第三方依赖，继续复用项目已有的 `framer-motion`、`animate.css` 和现有 UI 组件。
- 输入内容仍走后端既有校验链路，没有绕过长度和内容限制。
- 展示层和业务逻辑保持分离，后续若继续调整视觉结构，不需要改动接口逻辑。

## Validation
- Passed: `npm.cmd run lint` (`tsc --noEmit`)
