# 1.2.37

## Goal
- 调整右侧留言控制面板的信息卡布局，让 `CURRENT NICKNAME` 与 `LATEST SIGNAL` 并排显示，同时保持移动端可读性。
- 清理该组件中残留的异常文案，确保文字显示正常。

## Modified Files
- `src/components/GuestMessageBoard.tsx`
- `version/1.2.37.md` (new)

## Changes
- `src/components/GuestMessageBoard.tsx`
  - 将信息区网格改为响应式双列布局：
    - `sm` 及以上：`CURRENT NICKNAME` 与 `LATEST SIGNAL` 并排显示。
    - 小屏设备：仍保持上下堆叠，避免拥挤。
  - 让底部规则说明卡跨两列显示，维持信息层级清晰。
  - 修正该组件中的中文提示文案，避免乱码内容继续出现在输入区和信息卡内。
  - 未改动昵称校验、留言提交、错误反馈和回调同步逻辑。

## Preserved Behavior
- 访客模式仍然只依赖昵称，不新增注册流程。
- 留言仍然写入 Redis，提交协议未变化。
- 提交成功后仍通过 `onMessageCreated` 回推到 Home 中的弹幕展示区。

## Security / Maintainability
- 仅调整展示层网格结构，不新增依赖，不改动接口调用方式。
- 保持组件职责不变，后续继续调整视觉布局时不会影响留言业务流。

## Validation
- Passed: `npm.cmd run lint` (`tsc --noEmit`)
