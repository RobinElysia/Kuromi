# 1.2.39

## Goal
- 恢复 `Home` 页原有的 UI、排版、动效与视觉层，不再使用上一次重写后的替代样式。
- 保留此前为静态部署新增的数据接入能力，确保 `dist` 部署到 Nginx 后仍可读取最近帖子与留言数据。

## Modified Files
- `src/components/Home.tsx`
- `version/1.2.39.md`

## Changes
- `src/components/Home.tsx`
  - 将页面结构恢复为仓库原有版本，包括：
    - 顶部滚动丝带
    - Hero 主视觉区
    - Recent posts 区块
    - Personal Stack Map 区块
    - Some Whispers 区块
    - Life · Stories 区块
    - Home Board 弹幕留言区
    - 页脚流程提示
  - 保留原有 Kuromi 风格动画、拖拽贴纸、弹幕展示、翻牌卡片与分段视觉布局，不再改动整页样式语言。
  - 仅替换两处数据读取实现：
    - 最近帖子列表改为通过 `getPosts(3)` 读取
    - 留言板数据改为通过 `getMessages(MESSAGE_LIMIT)` 读取
  - 结果是：
    - 开发环境继续兼容原 `/api/*` 服务端链路
    - 静态生产环境继续兼容浏览器本地数据模式
    - 页面视觉表现回到你原先的 Home UI

## Review Focus
- 确认 `Home` 页面视觉是否已与原版本一致。
- 确认恢复原 UI 后，最近帖子和留言板在静态模式下仍可正常显示。
- 确认这次修改没有删除原有业务展示区块或交互动效。

## Validation
- `npm.cmd run lint`
  - 未通过。
  - 失败位置仍为 `node_modules/@vitejs/plugin-react/dist/index.d.ts`，属于现有第三方依赖类型声明解析问题，不是本次 `Home` 页面恢复造成的业务代码错误。
- `npm.cmd run build`
  - 本次会话内未完成二次验证。
  - 原因是沙箱环境执行 Vite 构建时触发 `spawn EPERM`，提权重跑请求被策略拒绝。
  - 本次对 `Home` 的改动仅为恢复原文件并替换两处数据读取入口，未新增复杂编译路径。
